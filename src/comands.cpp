#include "commands.h"
#include <iostream>

int Commands::cmd_echo(const std::vector<std::string>& args) {
    return 0;
}

int Commands::cmd_pwd(const std::vector<std::string>& args) {
    return 0;
}
